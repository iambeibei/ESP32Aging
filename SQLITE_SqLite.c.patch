--- lgw_v2_inspect/Light_Gateway_modified_v2/main/DataStorage/SqLite/SqLite.c	2026-09-09 03:54:27.000000000 +0000
+++ Light_Gateway_sqlite_fixed/main/DataStorage/SqLite/SqLite.c	2026-09-09 06:39:18.333127692 +0000
@@ -8,7 +8,7 @@
 
 #include "freertos/FreeRTOS.h"
 #include "freertos/task.h"
-#include "driver/gpio.h"
+#include "freertos/semphr.h"
 #include "esp_err.h"
 #include "esp_log.h"
 #include "esp_timer.h"
@@ -21,14 +21,19 @@
 #include "SqLite.h"
 
 static const char *TAG = "SQLITE";
-static wl_handle_t s_wl_handle = WL_INVALID_HANDLE;
 
-/* forward declarations used by corruption recovery */
-static int create_schema_v4(sqlite3 *db);
+static wl_handle_t s_wl_handle = WL_INVALID_HANDLE;
+static SemaphoreHandle_t s_sqlite_mutex = NULL;
+static sqlite3 *s_db = NULL;
+static bool s_db_ready = false;
 
 char db1_name[96];
 QueryResult g_db1_result = {0};
 
+/* -------------------------------------------------------------------------- */
+/* 基础工具                                                                    */
+/* -------------------------------------------------------------------------- */
+
 static int sqlite_now_seconds(void)
 {
     time_t now = time(NULL);
@@ -36,7 +41,6 @@
     {
         return (int)now;
     }
-
     return (int)(esp_timer_get_time() / 1000000LL);
 }
 
@@ -48,7 +52,6 @@
     }
 
     dst[0] = '\0';
-
     if (src == NULL)
     {
         return;
@@ -65,12 +68,7 @@
 
 static void fill_legacy_float_fields(QueryResult *result)
 {
-    if (result == NULL)
-    {
-        return;
-    }
-
-    if (result->value_data[0] == '[')
+    if (result == NULL || result->value_data[0] == '\0' || result->value_data[0] == '[')
     {
         return;
     }
@@ -85,8 +83,9 @@
 
     while (token != NULL && index < 4)
     {
-        values[index] = scaled_int_to_float(atoi(token));
-        index++;
+        char *equal = strchr(token, '=');
+        const char *number = equal ? equal + 1 : token;
+        values[index++] = scaled_int_to_float(atoi(number));
         token = strtok_r(NULL, ",", &saveptr);
     }
 
@@ -105,14 +104,13 @@
 
     memset(result, 0, sizeof(QueryResult));
 
+    /* 查询列顺序固定：seq_no, sn, current_step, timestamp, pushed, value_data, created_at */
     result->seq_no = sqlite3_column_int(stmt, 0);
 
     const unsigned char *sn = sqlite3_column_text(stmt, 1);
     safe_copy(result->SN, sizeof(result->SN), (const char *)sn);
 
     result->current_step = (short)sqlite3_column_int(stmt, 2);
-
-
     result->timestamp = sqlite3_column_int(stmt, 3);
     result->pushed = sqlite3_column_int(stmt, 4) ? true : false;
 
@@ -121,139 +119,94 @@
 
     result->created_at = sqlite3_column_int(stmt, 6);
 
-    /* legacy compatibility */
+    /* 兼容旧业务字段 */
     result->IDNUM = result->seq_no;
     fill_legacy_float_fields(result);
     (void)db_build_data_report_json(result, result->json_data, sizeof(result->json_data));
 }
 
-static int apply_sqlite_pragmas(sqlite3 *db)
+static bool sqlite_lock(void)
 {
-    if (db == NULL)
+    if (s_sqlite_mutex == NULL)
     {
-        return SQLITE_ERROR;
+        ESP_LOGE(TAG, "SQLite mutex is not initialized");
+        return false;
     }
 
-    /*
-     * 嵌入式 Flash/FATFS 上优先保证数据库一致性。
-     * MEMORY journal 在写事务中掉电/复位时无法提供持久回滚，
-     * 容易留下 SQLITE_CORRUPT。DELETE + FULL 会增加少量写放大，
-     * 但对于老化数据缓存更可靠。
-     */
-    int rc = SQLITE_OK;
-    if (db_exec(db, "PRAGMA journal_mode = DELETE;") != SQLITE_OK) rc = SQLITE_ERROR;
-    if (db_exec(db, "PRAGMA synchronous = FULL;") != SQLITE_OK) rc = SQLITE_ERROR;
-    if (db_exec(db, "PRAGMA temp_store = MEMORY;") != SQLITE_OK) rc = SQLITE_ERROR;
-    if (db_exec(db, "PRAGMA locking_mode = NORMAL;") != SQLITE_OK) rc = SQLITE_ERROR;
-    if (db_exec(db, "PRAGMA cache_size = -64;") != SQLITE_OK) rc = SQLITE_ERROR;
-    if (db_exec(db, "PRAGMA auto_vacuum = NONE;") != SQLITE_OK) rc = SQLITE_ERROR;
-    return rc;
-}
-
-/*
- * 快速检查数据库 B-Tree/page 是否完整。
- * 返回 SQLITE_OK 仅代表 quick_check 返回 "ok"。
- */
-static int sqlite_quick_check(sqlite3 *db)
-{
-    if (db == NULL)
+    if (xSemaphoreTake(s_sqlite_mutex, pdMS_TO_TICKS(10000)) != pdTRUE)
     {
-        return SQLITE_ERROR;
+        ESP_LOGE(TAG, "SQLite mutex timeout");
+        return false;
     }
 
-    sqlite3_stmt *stmt = NULL;
-    int rc = sqlite3_prepare_v2(db, "PRAGMA quick_check;", -1, &stmt, NULL);
-    if (rc != SQLITE_OK)
-    {
-        ESP_LOGE(TAG, "SQLite quick_check prepare failed: rc=%d, errmsg=%s",
-                 rc, sqlite3_errmsg(db));
-        return rc;
-    }
+    return true;
+}
 
-    rc = sqlite3_step(stmt);
-    if (rc == SQLITE_ROW)
+static void sqlite_unlock(void)
+{
+    if (s_sqlite_mutex != NULL)
     {
-        const unsigned char *text = sqlite3_column_text(stmt, 0);
-        bool ok = (text != NULL && strcmp((const char *)text, "ok") == 0);
-        if (!ok)
-        {
-            ESP_LOGE(TAG, "SQLite quick_check failed: %s",
-                     text ? (const char *)text : "unknown");
-            sqlite3_finalize(stmt);
-            return SQLITE_CORRUPT;
-        }
-
-        sqlite3_finalize(stmt);
-        return SQLITE_OK;
+        xSemaphoreGive(s_sqlite_mutex);
     }
-
-    ESP_LOGE(TAG, "SQLite quick_check step failed: rc=%d, errmsg=%s",
-             rc, sqlite3_errmsg(db));
-    sqlite3_finalize(stmt);
-    return (rc == SQLITE_DONE) ? SQLITE_CORRUPT : rc;
 }
 
-/*
- * 数据库损坏时只清理 SQLite 自身文件，不格式化整个 data_storage 分区。
- * 这会丢失已损坏数据库里的历史/待补发记录，但不会影响 NVS/SPIFFS。
- */
-static int recreate_corrupt_database(void)
+static bool sqlite_ready_locked(void)
 {
-    ESP_LOGE(TAG, "SQLite database is corrupt, recreating %s", DB_FILE_PATH);
-
-    cleanup_database_files();
-
-    sqlite3 *db = NULL;
-    int rc = db_open(DB_FILE_PATH, &db);
-    if (rc != SQLITE_OK)
+    if (!s_db_ready || s_db == NULL)
     {
-        ESP_LOGE(TAG, "Recreate database open failed: rc=%d", rc);
-        if (db != NULL)
-        {
-            sqlite3_close(db);
-        }
-        return rc;
+        ESP_LOGE(TAG, "SQLite database is not ready");
+        return false;
     }
+    return true;
+}
 
-    /* 新库创建表前设置 page_size。 */
-    if (db_exec(db, "PRAGMA page_size = 1024;") != SQLITE_OK)
-    {
-        sqlite3_close(db);
-        return SQLITE_ERROR;
-    }
+static void sqlite_log_file_state(void)
+{
+    struct stat st = {0};
 
-    if (apply_sqlite_pragmas(db) != SQLITE_OK)
+    if (stat(FATFS_BASE_PATH, &st) == 0)
     {
-        sqlite3_close(db);
-        return SQLITE_ERROR;
+        ESP_LOGI(TAG, "%s is accessible", FATFS_BASE_PATH);
     }
-
-    rc = create_schema_v4(db);
-    if (rc == SQLITE_OK)
+    else
     {
-        rc = sqlite_quick_check(db);
+        ESP_LOGE(TAG, "%s is not accessible, errno=%d", FATFS_BASE_PATH, errno);
     }
 
-    sqlite3_close(db);
-
-    if (rc == SQLITE_OK)
+    if (stat(DB_FILE_PATH, &st) == 0)
     {
-        ESP_LOGW(TAG, "SQLite database recreated successfully");
+        ESP_LOGI(TAG, "%s exists, size=%ld", DB_FILE_PATH, (long)st.st_size);
     }
     else
     {
-        ESP_LOGE(TAG, "SQLite database recreate failed: rc=%d", rc);
+        ESP_LOGW(TAG, "%s does not exist/access failed, errno=%d", DB_FILE_PATH, errno);
     }
 
-    return rc;
+    if (stat(DB_FILE_PATH "-journal", &st) == 0)
+    {
+        ESP_LOGW(TAG, "Hot/leftover journal present: %s-journal, size=%ld",
+                 DB_FILE_PATH, (long)st.st_size);
+    }
 }
 
-/// @brief 挂载 FatFS 存储
+/* -------------------------------------------------------------------------- */
+/* FATFS                                                                        */
+/* -------------------------------------------------------------------------- */
+
 esp_err_t mount_fatfs_storage(void)
 {
+    if (s_wl_handle != WL_INVALID_HANDLE)
+    {
+        return ESP_OK;
+    }
+
+    /*
+     * 掉电恢复数据库绝不能自动格式化。
+     * 挂载失败时保留原分区，返回错误，由上层记录故障。
+     */
     esp_vfs_fat_mount_config_t mount_config = {
-        .format_if_mount_failed = true,
-        .max_files = 2,
+        .format_if_mount_failed = false,
+        .max_files = 8,
         .allocation_unit_size = 4096,
     };
 
@@ -265,7 +218,9 @@
 
     if (ret != ESP_OK)
     {
-        ESP_LOGE(TAG, "Failed to mount FATFS: %s", esp_err_to_name(ret));
+        s_wl_handle = WL_INVALID_HANDLE;
+        ESP_LOGE(TAG, "Failed to mount FATFS: %s; storage is preserved and will NOT be formatted",
+                 esp_err_to_name(ret));
         return ret;
     }
 
@@ -273,66 +228,37 @@
     return ESP_OK;
 }
 
-/// @brief 卸载 FatFS 存储
 void unmount_fatfs_storage(void)
 {
-    if (s_wl_handle != WL_INVALID_HANDLE)
+    if (!sqlite_lock())
     {
-        esp_vfs_fat_spiflash_unmount_rw_wl(FATFS_BASE_PATH, s_wl_handle);
-        s_wl_handle = WL_INVALID_HANDLE;
-        ESP_LOGI(TAG, "FATFS unmounted");
+        return;
     }
-}
 
-/// @brief 获取并打印实际 Flash 芯片总容量
-static void print_real_flash_size(void)
-{
-    uint32_t flash_size = 0;
+    s_db_ready = false;
 
-    esp_err_t ret = esp_flash_get_physical_size(NULL, &flash_size);
-    if (ret == ESP_OK)
-    {
-        printf("Real Flash Chip Size: %lu bytes (%.2f MB)\n",
-               (unsigned long)flash_size,
-               flash_size / (1024.0 * 1024.0));
-    }
-    else
+    if (s_db != NULL)
     {
-        printf("Failed to get flash size: %s\n", esp_err_to_name(ret));
+        int rc = sqlite3_close(s_db);
+        if (rc != SQLITE_OK)
+        {
+            ESP_LOGE(TAG, "sqlite3_close failed before unmount: rc=%d; FATFS remains mounted", rc);
+            sqlite_unlock();
+            return;
+        }
+        s_db = NULL;
     }
-}
 
-/// @brief 打印分区表信息
-static void print_all_partitions(void)
-{
-    esp_partition_iterator_t it = esp_partition_find(
-        ESP_PARTITION_TYPE_ANY,
-        ESP_PARTITION_SUBTYPE_ANY,
-        NULL);
-
-    printf("\n========== Partition Table Info ==========\n");
-    printf("%-15s %-10s %-12s %-16s %-12s\n",
-           "Name", "Type", "SubType", "Offset", "Size");
-    printf("--------------------------------------------------------\n");
-
-    for (; it != NULL; it = esp_partition_next(it))
+    if (s_wl_handle != WL_INVALID_HANDLE)
     {
-        const esp_partition_t *p = esp_partition_get(it);
-
-        printf("%-15s %-10d %-12d 0x%08lX   %-7lu (%.2f KB)\n",
-               p->label,
-               p->type,
-               p->subtype,
-               (unsigned long)p->address,
-               (unsigned long)p->size,
-               p->size / 1024.0);
+        esp_vfs_fat_spiflash_unmount_rw_wl(FATFS_BASE_PATH, s_wl_handle);
+        s_wl_handle = WL_INVALID_HANDLE;
+        ESP_LOGI(TAG, "FATFS unmounted");
     }
 
-    esp_partition_iterator_release(it);
-    printf("==========================================\n");
+    sqlite_unlock();
 }
 
-/// @brief 打印 FatFS 使用情况
 void print_fatfs_usage(void)
 {
     uint64_t total_bytes = 0;
@@ -351,41 +277,17 @@
     {
         printf("Partition Label:   %s\n", partition->label);
         printf("Partition Offset:  0x%08lX\n", (unsigned long)partition->address);
-        printf("Partition Size:    %lu bytes (%.2f KB, %.2f MB)\n",
+        printf("Partition Size:    %lu bytes (%.2f MB)\n",
                (unsigned long)partition->size,
-               partition->size / 1024.0,
                partition->size / (1024.0 * 1024.0));
-        printf("----------------------------\n");
-    }
-    else
-    {
-        printf("Partition not found: label=%s, subtype=FAT\n", FATFS_PARTITION_LABEL);
     }
 
     if (ret == ESP_OK)
     {
         uint64_t used_bytes = total_bytes - free_bytes;
-
-        printf("File System Total: %llu bytes (%.2f KB, %.2f MB)\n",
-               (unsigned long long)total_bytes,
-               total_bytes / 1024.0,
-               total_bytes / (1024.0 * 1024.0));
-
-        printf("File System Used:  %llu bytes (%.2f KB, %.2f MB)\n",
-               (unsigned long long)used_bytes,
-               used_bytes / 1024.0,
-               used_bytes / (1024.0 * 1024.0));
-
-        printf("File System Free:  %llu bytes (%.2f KB, %.2f MB)\n",
-               (unsigned long long)free_bytes,
-               free_bytes / 1024.0,
-               free_bytes / (1024.0 * 1024.0));
-
-        if (total_bytes > 0)
-        {
-            printf("Usage Ratio:       %.2f%%\n",
-                   (double)used_bytes / (double)total_bytes * 100.0);
-        }
+        printf("File System Total: %llu bytes\n", (unsigned long long)total_bytes);
+        printf("File System Used:  %llu bytes\n", (unsigned long long)used_bytes);
+        printf("File System Free:  %llu bytes\n", (unsigned long long)free_bytes);
     }
     else
     {
@@ -393,235 +295,339 @@
     }
 }
 
-/// @brief 删除数据库相关文件。调试清库时使用，正式运行一般不要调用。
-void cleanup_database_files(void)
+void print_db_files(void)
 {
-    unlink(FATFS_BASE_PATH "/data.db");
-    unlink(FATFS_BASE_PATH "/data.db-journal");
-    unlink(FATFS_BASE_PATH "/data.db-wal");
-    unlink(FATFS_BASE_PATH "/data.db-shm");
-    unlink(FATFS_BASE_PATH "/data.jou");
-    unlink(FATFS_BASE_PATH "/raw.bin");
-    unlink(FATFS_BASE_PATH "/rand.bin");
+    struct stat st = {0};
+    if (stat(DB_FILE_PATH, &st) == 0)
+    {
+        printf("%s size: %ld bytes (%.2f KB)\n",
+               DB_FILE_PATH,
+               (long)st.st_size,
+               st.st_size / 1024.0);
+    }
+    else
+    {
+        printf("%s not found, errno=%d\n", DB_FILE_PATH, errno);
+    }
 }
 
-static int get_user_version(sqlite3 *db)
+/*
+ * 仅保留为人工调试/维修接口。
+ * 正常初始化、quick_check失败、运行时错误都绝不会自动调用本函数。
+ */
+void cleanup_database_files(void)
 {
-    sqlite3_stmt *stmt = NULL;
-    int version = 0;
-
-    int rc = sqlite3_prepare_v2(db, "PRAGMA user_version;", -1, &stmt, NULL);
-    if (rc != SQLITE_OK)
+    if (!sqlite_lock())
     {
-        return 0;
+        return;
     }
 
-    if (sqlite3_step(stmt) == SQLITE_ROW)
+    s_db_ready = false;
+
+    if (s_db != NULL)
     {
-        version = sqlite3_column_int(stmt, 0);
+        (void)sqlite3_close(s_db);
+        s_db = NULL;
     }
 
-    sqlite3_finalize(stmt);
-    return version;
+    unlink(DB_FILE_PATH);
+    unlink(DB_FILE_PATH "-journal");
+    unlink(DB_FILE_PATH "-wal");
+    unlink(DB_FILE_PATH "-shm");
+
+    ESP_LOGW(TAG, "SQLite database files were manually removed");
+    sqlite_unlock();
 }
 
-static void print_sqlite_pragmas(sqlite3 *db)
+/* -------------------------------------------------------------------------- */
+/* SQLite初始化、完整性与schema                                                 */
+/* -------------------------------------------------------------------------- */
+
+static int apply_sqlite_pragmas(sqlite3 *db)
 {
-    sqlite3_stmt *stmt = NULL;
+    if (db == NULL)
+    {
+        return SQLITE_MISUSE;
+    }
 
-    const char *sql =
-        "SELECT "
-        "(SELECT page_size FROM pragma_page_size), "
-        "(SELECT page_count FROM pragma_page_count), "
-        "(SELECT max_page_count FROM pragma_max_page_count), "
-        "(SELECT freelist_count FROM pragma_freelist_count);";
+    /*
+     * DELETE journal + FULL synchronous：
+     * 先把回滚日志持久化，再提交数据库页。突然断电后由 SQLite 自动恢复 hot journal。
+     */
+    if (db_exec(db, "PRAGMA journal_mode = DELETE;") != SQLITE_OK) return SQLITE_ERROR;
+    if (db_exec(db, "PRAGMA synchronous = FULL;") != SQLITE_OK) return SQLITE_ERROR;
+    if (db_exec(db, "PRAGMA temp_store = MEMORY;") != SQLITE_OK) return SQLITE_ERROR;
+    if (db_exec(db, "PRAGMA locking_mode = NORMAL;") != SQLITE_OK) return SQLITE_ERROR;
+    if (db_exec(db, "PRAGMA cache_size = -64;") != SQLITE_OK) return SQLITE_ERROR;
+    if (db_exec(db, "PRAGMA auto_vacuum = NONE;") != SQLITE_OK) return SQLITE_ERROR;
+    if (db_exec(db, "PRAGMA busy_timeout = 5000;") != SQLITE_OK) return SQLITE_ERROR;
 
-    int rc = sqlite3_prepare_v2(db, sql, -1, &stmt, NULL);
+    return SQLITE_OK;
+}
+
+static int sqlite_basic_check(sqlite3 *db)
+{
+    sqlite3_stmt *stmt = NULL;
+    int rc = sqlite3_prepare_v2(db, "SELECT COUNT(*) FROM sqlite_master;", -1, &stmt, NULL);
     if (rc != SQLITE_OK)
     {
-        printf("prepare pragma failed: %s\n", sqlite3_errmsg(db));
-        return;
+        ESP_LOGE(TAG, "SQLite basic check prepare failed: rc=%d, errmsg=%s",
+                 rc, sqlite3_errmsg(db));
+        return rc;
     }
 
     rc = sqlite3_step(stmt);
     if (rc == SQLITE_ROW)
     {
-        printf("\n=== SQLite PRAGMA Info ===\n");
-        printf("page_size      = %d\n", sqlite3_column_int(stmt, 0));
-        printf("page_count     = %d\n", sqlite3_column_int(stmt, 1));
-        printf("max_page_count = %d\n", sqlite3_column_int(stmt, 2));
-        printf("freelist_count = %d\n", sqlite3_column_int(stmt, 3));
-        printf("==========================\n");
-    }
-    else
-    {
-        printf("read pragma failed: rc=%d, msg=%s\n", rc, sqlite3_errmsg(db));
+        sqlite3_finalize(stmt);
+        return SQLITE_OK;
     }
 
+    ESP_LOGE(TAG, "SQLite basic check failed: rc=%d, errmsg=%s",
+             rc, sqlite3_errmsg(db));
     sqlite3_finalize(stmt);
+    return rc;
 }
 
-static void print_file_size(const char *path)
+/*
+ * 某些嵌入式 SQLite 编译配置下 PRAGMA quick_check 可能不返回结果行而直接 DONE。
+ * SQLITE_DONE(101) 是正常结束，不是 SQLITE_CORRUPT(11)。
+ * 无结果行时退化为 sqlite_master 基础可读性检查。
+ */
+static int sqlite_quick_check(sqlite3 *db)
 {
-    struct stat st = {0};
-
-    if (stat(path, &st) == 0)
-    {
-        printf("%s size: %ld bytes (%.2f KB)\n",
-               path,
-               (long)st.st_size,
-               st.st_size / 1024.0);
-    }
-    else
+    sqlite3_stmt *stmt = NULL;
+    int rc = sqlite3_prepare_v2(db, "PRAGMA quick_check;", -1, &stmt, NULL);
+    if (rc != SQLITE_OK)
     {
-        printf("%s not found, errno=%d\n", path, errno);
+        ESP_LOGE(TAG, "SQLite quick_check prepare failed: rc=%d, errmsg=%s",
+                 rc, sqlite3_errmsg(db));
+        return rc;
     }
-}
 
-/// @brief 打印数据库文件大小
-void print_db_files(void)
-{
-    print_file_size(DB_FILE_PATH);
-}
+    bool got_row = false;
 
-static int create_schema_v4(sqlite3 *db)
-{
-    if (db == NULL)
+    while ((rc = sqlite3_step(stmt)) == SQLITE_ROW)
     {
-        return SQLITE_ERROR;
+        got_row = true;
+        const unsigned char *text = sqlite3_column_text(stmt, 0);
+
+        if (text == NULL || strcmp((const char *)text, "ok") != 0)
+        {
+            ESP_LOGE(TAG, "SQLite quick_check reported: %s",
+                     text ? (const char *)text : "NULL");
+            sqlite3_finalize(stmt);
+            return SQLITE_CORRUPT;
+        }
     }
 
-    int rc = db_exec(db,
-                     "CREATE TABLE IF NOT EXISTS " SQLITE_CACHE_TABLE_NAME " ("
-                     "seq_no INTEGER PRIMARY KEY,"
-                     "sn TEXT NOT NULL CHECK(length(sn) < 64),"
-                     "current_step INTEGER NOT NULL,"
-                     "timestamp INTEGER NOT NULL,"
-                     "pushed INTEGER NOT NULL DEFAULT 0 CHECK(pushed IN (0, 1)),"
-                     "value_data TEXT NOT NULL CHECK(length(CAST(value_data AS BLOB)) <= 2048),"
-                     "created_at INTEGER NOT NULL"
-                     ");");
-    if (rc != SQLITE_OK)
+    if (rc != SQLITE_DONE)
     {
+        ESP_LOGE(TAG, "SQLite quick_check execution failed: rc=%d, errmsg=%s",
+                 rc, sqlite3_errmsg(db));
+        sqlite3_finalize(stmt);
         return rc;
     }
 
-    rc = db_exec(db,
-                 "CREATE INDEX IF NOT EXISTS idx_data_cache_pushed_time "
-                 "ON " SQLITE_CACHE_TABLE_NAME " (pushed, created_at);");
-    if (rc != SQLITE_OK)
+    sqlite3_finalize(stmt);
+
+    if (got_row)
     {
-        return rc;
+        ESP_LOGI(TAG, "SQLite quick_check OK");
+        return SQLITE_OK;
     }
 
-    rc = db_exec(db,
-                 "CREATE INDEX IF NOT EXISTS idx_data_cache_created_at "
-                 "ON " SQLITE_CACHE_TABLE_NAME " (created_at);");
-    if (rc != SQLITE_OK)
+    ESP_LOGW(TAG, "SQLite quick_check returned no rows; using basic database check");
+    return sqlite_basic_check(db);
+}
+
+static bool sqlite_table_exists(sqlite3 *db, const char *table_name)
+{
+    sqlite3_stmt *stmt = NULL;
+    const char *sql = "SELECT 1 FROM sqlite_master WHERE type='table' AND name=? LIMIT 1;";
+
+    if (sqlite3_prepare_v2(db, sql, -1, &stmt, NULL) != SQLITE_OK)
     {
-        return rc;
+        return false;
     }
 
-    return db_exec(db, "PRAGMA user_version = 4;");
+    sqlite3_bind_text(stmt, 1, table_name, -1, SQLITE_TRANSIENT);
+    bool exists = (sqlite3_step(stmt) == SQLITE_ROW);
+    sqlite3_finalize(stmt);
+    return exists;
 }
 
-static bool sqlite_table_exists(sqlite3 *db, const char *table_name)
+static bool sqlite_table_has_column(sqlite3 *db, const char *table_name, const char *column_name)
 {
-    if (db == NULL || table_name == NULL)
+    if (db == NULL || table_name == NULL || column_name == NULL)
     {
         return false;
     }
 
+    char sql[128];
+    snprintf(sql, sizeof(sql), "PRAGMA table_info(%s);", table_name);
+
     sqlite3_stmt *stmt = NULL;
-    const char *sql = "SELECT 1 FROM sqlite_master WHERE type='table' AND name=? LIMIT 1;";
     if (sqlite3_prepare_v2(db, sql, -1, &stmt, NULL) != SQLITE_OK)
     {
         return false;
     }
 
-    sqlite3_bind_text(stmt, 1, table_name, -1, SQLITE_TRANSIENT);
-    bool exists = (sqlite3_step(stmt) == SQLITE_ROW);
+    bool found = false;
+    int rc;
+    while ((rc = sqlite3_step(stmt)) == SQLITE_ROW)
+    {
+        const unsigned char *name = sqlite3_column_text(stmt, 1);
+        if (name != NULL && strcmp((const char *)name, column_name) == 0)
+        {
+            found = true;
+            break;
+        }
+    }
+
     sqlite3_finalize(stmt);
-    return exists;
+    return found;
 }
 
-/*
- * schema v4 扩大 SN/Value 容量，并把 value_data 的语义改为结构化 Value JSON。
- * 旧 v3 数据先原样迁移；db_build_data_report_json() 对旧 key=value/CSV 记录
- * 仍提供兼容转换，因此升级不会直接丢失历史缓存。
- */
-static int migrate_schema_to_v4(sqlite3 *db, int current_version)
+static int get_user_version(sqlite3 *db)
 {
-    if (db == NULL)
+    sqlite3_stmt *stmt = NULL;
+    int version = 0;
+
+    if (sqlite3_prepare_v2(db, "PRAGMA user_version;", -1, &stmt, NULL) != SQLITE_OK)
+    {
+        return 0;
+    }
+
+    if (sqlite3_step(stmt) == SQLITE_ROW)
     {
-        return SQLITE_ERROR;
+        version = sqlite3_column_int(stmt, 0);
     }
 
-    if (current_version >= SQLITE_SCHEMA_VERSION)
+    sqlite3_finalize(stmt);
+    return version;
+}
+
+static int create_schema_v5(sqlite3 *db)
+{
+    if (db == NULL)
     {
-        return create_schema_v4(db);
+        return SQLITE_MISUSE;
     }
 
+    /*
+     * local_id 是数据库内部主键；IDNUM(seq_no)只在同一个SN内唯一。
+     * 这修复了“第二台设备IDNUM重新从0开始时覆盖第一台设备记录”的问题。
+     */
+    int rc = db_exec(db,
+                     "CREATE TABLE IF NOT EXISTS " SQLITE_CACHE_TABLE_NAME " ("
+                     "local_id INTEGER PRIMARY KEY,"
+                     "seq_no INTEGER NOT NULL,"
+                     "sn TEXT NOT NULL CHECK(length(sn) < 64),"
+                     "current_step INTEGER NOT NULL,"
+                     "timestamp INTEGER NOT NULL,"
+                     "pushed INTEGER NOT NULL DEFAULT 0 CHECK(pushed IN (0,1)),"
+                     "value_data TEXT NOT NULL CHECK(length(CAST(value_data AS BLOB)) <= 2048),"
+                     "created_at INTEGER NOT NULL,"
+                     "UNIQUE(sn, seq_no)"
+                     ");");
+    if (rc != SQLITE_OK) return rc;
+
+    rc = db_exec(db,
+                 "CREATE INDEX IF NOT EXISTS idx_cache_sn_seq_v5 "
+                 "ON " SQLITE_CACHE_TABLE_NAME " (sn, seq_no DESC);");
+    if (rc != SQLITE_OK) return rc;
+
+    rc = db_exec(db,
+                 "CREATE INDEX IF NOT EXISTS idx_cache_sn_push_seq_v5 "
+                 "ON " SQLITE_CACHE_TABLE_NAME " (sn, pushed, seq_no);");
+    if (rc != SQLITE_OK) return rc;
+
+    rc = db_exec(db,
+                 "CREATE INDEX IF NOT EXISTS idx_cache_push_local_v5 "
+                 "ON " SQLITE_CACHE_TABLE_NAME " (pushed, local_id);");
+    if (rc != SQLITE_OK) return rc;
+
+    return db_exec(db, "PRAGMA user_version = 5;");
+}
+
+static int migrate_schema_to_v5(sqlite3 *db, int current_version)
+{
     if (!sqlite_table_exists(db, SQLITE_CACHE_TABLE_NAME))
     {
-        return create_schema_v4(db);
+        return create_schema_v5(db);
     }
 
+    bool has_local_id = sqlite_table_has_column(db, SQLITE_CACHE_TABLE_NAME, "local_id");
+    if (current_version >= SQLITE_SCHEMA_VERSION && has_local_id)
+    {
+        return create_schema_v5(db);
+    }
+
+    ESP_LOGW(TAG, "Migrating SQLite schema from v%d to v%d",
+             current_version, SQLITE_SCHEMA_VERSION);
+
     int rc = db_exec(db, "BEGIN IMMEDIATE TRANSACTION;");
     if (rc != SQLITE_OK)
     {
         return rc;
     }
 
-    rc = db_exec(db, "DROP INDEX IF EXISTS idx_data_cache_pushed_time;");
+    rc = db_exec(db, "DROP TABLE IF EXISTS data_cache_pre_v5_backup;");
     if (rc != SQLITE_OK) goto rollback;
-    rc = db_exec(db, "DROP INDEX IF EXISTS idx_data_cache_created_at;");
-    if (rc != SQLITE_OK) goto rollback;
-    rc = db_exec(db, "DROP TABLE IF EXISTS data_cache_v3_backup;");
-    if (rc != SQLITE_OK) goto rollback;
-    rc = db_exec(db, "ALTER TABLE " SQLITE_CACHE_TABLE_NAME " RENAME TO data_cache_v3_backup;");
+
+    rc = db_exec(db,
+                 "ALTER TABLE " SQLITE_CACHE_TABLE_NAME " RENAME TO data_cache_pre_v5_backup;");
     if (rc != SQLITE_OK) goto rollback;
 
-    rc = create_schema_v4(db);
+    rc = create_schema_v5(db);
     if (rc != SQLITE_OK) goto rollback;
 
+    /* v3/v4均包含以下七列，按原值迁移。 */
     rc = db_exec(db,
                  "INSERT OR REPLACE INTO " SQLITE_CACHE_TABLE_NAME " "
                  "(seq_no, sn, current_step, timestamp, pushed, value_data, created_at) "
                  "SELECT seq_no, sn, current_step, timestamp, pushed, value_data, created_at "
-                 "FROM data_cache_v3_backup "
-                 "WHERE length(sn) < 64 AND length(CAST(value_data AS BLOB)) <= 2048;");
+                 "FROM data_cache_pre_v5_backup "
+                 "WHERE length(sn) < 64 "
+                 "AND length(CAST(value_data AS BLOB)) <= 2048;");
+    if (rc != SQLITE_OK) goto rollback;
+
+    rc = db_exec(db, "DROP TABLE data_cache_pre_v5_backup;");
     if (rc != SQLITE_OK) goto rollback;
 
-    rc = db_exec(db, "DROP TABLE data_cache_v3_backup;");
+    rc = db_exec(db, "PRAGMA user_version = 5;");
     if (rc != SQLITE_OK) goto rollback;
 
     rc = db_exec(db, "COMMIT;");
     if (rc == SQLITE_OK)
     {
-        ESP_LOGI(TAG, "SQLite schema migrated to v4");
+        ESP_LOGI(TAG, "SQLite schema migrated to v5");
     }
     return rc;
 
 rollback:
-    db_exec(db, "ROLLBACK;");
-    ESP_LOGE(TAG, "SQLite schema migration to v4 failed: %s", sqlite3_errmsg(db));
+    (void)db_exec(db, "ROLLBACK;");
+    ESP_LOGE(TAG, "SQLite schema migration failed: rc=%d, errmsg=%s",
+             rc, sqlite3_errmsg(db));
     return rc;
 }
 
-static int get_record_count(sqlite3 *db, int *record_count)
+static int get_record_count_locked(int *record_count)
 {
-    if (db == NULL || record_count == NULL)
+    if (record_count == NULL || s_db == NULL)
     {
         return -1;
     }
 
     *record_count = 0;
-
     sqlite3_stmt *stmt = NULL;
-    const char *sql = "SELECT COUNT(*) FROM " SQLITE_CACHE_TABLE_NAME ";";
 
-    int rc = sqlite3_prepare_v2(db, sql, -1, &stmt, NULL);
+    int rc = sqlite3_prepare_v2(s_db,
+                                "SELECT COUNT(*) FROM " SQLITE_CACHE_TABLE_NAME ";",
+                                -1,
+                                &stmt,
+                                NULL);
     if (rc != SQLITE_OK)
     {
         return -1;
@@ -630,204 +636,147 @@
     if (sqlite3_step(stmt) == SQLITE_ROW)
     {
         *record_count = sqlite3_column_int(stmt, 0);
+        sqlite3_finalize(stmt);
+        return 0;
     }
 
     sqlite3_finalize(stmt);
-    return 0;
+    return -1;
 }
 
-/// @brief 初始化数据库并创建/迁移缓存表。检测到损坏时自动重建 SQLite 文件。
-static void init_databases(void)
+static void print_sqlite_pragmas(sqlite3 *db)
 {
-    snprintf(db1_name, sizeof(db1_name), "%s", DB_FILE_PATH);
-
-    sqlite3 *db1 = NULL;
-    sqlite3_initialize();
+    sqlite3_stmt *stmt = NULL;
+    const char *sql =
+        "SELECT "
+        "(SELECT page_size FROM pragma_page_size), "
+        "(SELECT page_count FROM pragma_page_count), "
+        "(SELECT freelist_count FROM pragma_freelist_count);";
 
-    int rc = db_open(db1_name, &db1);
-    if (rc != SQLITE_OK)
+    if (sqlite3_prepare_v2(db, sql, -1, &stmt, NULL) != SQLITE_OK)
     {
-        printf("Failed to open database %s, rc=%d\n", db1_name, rc);
-
-        /* SQLITE_NOTADB/SQLITE_CORRUPT 等情况下尝试只重建数据库文件。 */
-        if (db1 != NULL)
-        {
-            sqlite3_close(db1);
-            db1 = NULL;
-        }
-
-        if (recreate_corrupt_database() != SQLITE_OK)
-        {
-            ESP_LOGE(TAG, "SQLite initialization failed after recreate");
-        }
         return;
     }
 
-    sqlite3_extended_result_codes(db1, 1);
-
-    /*
-     * 先检查旧数据库。损坏的库不能继续做 schema migration，
-     * 否则后续 INSERT 会持续得到 rc=11(SQLITE_CORRUPT)。
-     */
-    rc = sqlite_quick_check(db1);
-    if (rc != SQLITE_OK)
+    if (sqlite3_step(stmt) == SQLITE_ROW)
     {
-        sqlite3_close(db1);
-        db1 = NULL;
-        (void)recreate_corrupt_database();
-        return;
+        ESP_LOGI(TAG, "page_size=%d page_count=%d freelist_count=%d",
+                 sqlite3_column_int(stmt, 0),
+                 sqlite3_column_int(stmt, 1),
+                 sqlite3_column_int(stmt, 2));
     }
 
-    /* page_size 只会在新建数据库且创建表之前生效。 */
-    (void)db_exec(db1, "PRAGMA page_size = 1024;");
-
-    if (apply_sqlite_pragmas(db1) != SQLITE_OK)
-    {
-        ESP_LOGE(TAG, "Failed to apply SQLite pragmas");
-        sqlite3_close(db1);
-        return;
-    }
+    sqlite3_finalize(stmt);
+}
 
-    int version = get_user_version(db1);
-    printf("SQLite current user_version = %d, target = %d\n",
-           version,
-           SQLITE_SCHEMA_VERSION);
+static int init_database_locked(void)
+{
+    snprintf(db1_name, sizeof(db1_name), "%s", DB_FILE_PATH);
+    s_db_ready = false;
 
-    rc = migrate_schema_to_v4(db1, version);
-    if (rc != SQLITE_OK)
-    {
-        int err = sqlite3_errcode(db1);
-        ESP_LOGE(TAG, "Failed to create/migrate SQLite schema v4: rc=%d err=%d errmsg=%s",
-                 rc, err, sqlite3_errmsg(db1));
+    sqlite_log_file_state();
 
-        sqlite3_close(db1);
-        db1 = NULL;
+    struct stat st = {0};
+    bool db_existed_before_open = (stat(DB_FILE_PATH, &st) == 0);
 
-        if (rc == SQLITE_CORRUPT || err == SQLITE_CORRUPT ||
-            rc == SQLITE_NOTADB || err == SQLITE_NOTADB)
-        {
-            (void)recreate_corrupt_database();
-        }
-        return;
-    }
+    sqlite3_initialize();
 
-    /* migration 后再检查一次，避免把结构异常带到运行阶段。 */
-    rc = sqlite_quick_check(db1);
+    int rc = db_open(DB_FILE_PATH, &s_db);
     if (rc != SQLITE_OK)
     {
-        sqlite3_close(db1);
-        db1 = NULL;
-        (void)recreate_corrupt_database();
-        return;
+        ESP_LOGE(TAG, "Failed to open database %s: rc=%d", DB_FILE_PATH, rc);
+        sqlite_log_file_state();
+        return rc;
     }
 
-    int cache_count = 0;
-    if (get_record_count(db1, &cache_count) == 0)
+    /* 新库创建任何表之前设置为与 FATFS allocation unit 一致的4KB页。 */
+    if (!db_existed_before_open)
     {
-        printf("SQLite cache record count = %d\n", cache_count);
-    }
-
-    print_sqlite_pragmas(db1);
-    sqlite3_close(db1);
-
-    printf("Database initialized successfully: %s\n", db1_name);
-}
-
-
-
-static int get_existing_seq_no(sqlite3 *db, int seq_no, int *exists)
-{
-    if (db == NULL || exists == NULL)
-    {
-        return -1;
+        rc = db_exec(s_db, "PRAGMA page_size = 4096;");
+        if (rc != SQLITE_OK)
+        {
+            ESP_LOGE(TAG, "Failed to set page_size for new database");
+            goto fail;
+        }
     }
 
-    *exists = 0;
-
-    sqlite3_stmt *stmt = NULL;
-    const char *sql = "SELECT 1 FROM " SQLITE_CACHE_TABLE_NAME " WHERE seq_no = ? LIMIT 1;";
-
-    int rc = sqlite3_prepare_v2(db, sql, -1, &stmt, NULL);
+    rc = apply_sqlite_pragmas(s_db);
     if (rc != SQLITE_OK)
     {
-        return -1;
+        ESP_LOGE(TAG, "Failed to apply SQLite pragmas");
+        goto fail;
     }
 
-    sqlite3_bind_int(stmt, 1, seq_no);
-
-    rc = sqlite3_step(stmt);
-    if (rc == SQLITE_ROW)
+    /*
+     * sqlite3_open + 第一次读取会自动处理 DELETE journal 模式遗留的 hot journal。
+     * 此处只检查，不自动删除数据库或 journal。
+     */
+    rc = sqlite_quick_check(s_db);
+    if (rc != SQLITE_OK)
     {
-        *exists = 1;
+        ESP_LOGE(TAG,
+                 "SQLite integrity check failed: rc=%d. Database is preserved; no automatic delete/reformat.",
+                 rc);
+        goto fail;
     }
 
-    sqlite3_finalize(stmt);
-    return 0;
-}
+    int version = get_user_version(s_db);
+    ESP_LOGI(TAG, "SQLite current user_version=%d, target=%d",
+             version, SQLITE_SCHEMA_VERSION);
 
-static int get_overwrite_seq_no(sqlite3 *db, int *overwrite_seq_no)
-{
-    if (db == NULL || overwrite_seq_no == NULL)
+    rc = migrate_schema_to_v5(s_db, version);
+    if (rc != SQLITE_OK)
     {
-        return -1;
+        ESP_LOGE(TAG,
+                 "SQLite schema create/migration failed: rc=%d, errmsg=%s. Database is preserved.",
+                 rc,
+                 sqlite3_errmsg(s_db));
+        goto fail;
     }
 
-    *overwrite_seq_no = -1;
-
-    sqlite3_stmt *stmt = NULL;
-
-    /*
-     * 缓存满时优先覆盖 pushed=1 的旧记录。
-     * 若全部都是 pushed=0，则只能覆盖最早的未推送记录。
-     */
-    const char *sql =
-        "SELECT seq_no FROM " SQLITE_CACHE_TABLE_NAME " "
-        "ORDER BY pushed DESC, created_at ASC, seq_no ASC "
-        "LIMIT 1;";
-
-    int rc = sqlite3_prepare_v2(db, sql, -1, &stmt, NULL);
+    rc = sqlite_quick_check(s_db);
     if (rc != SQLITE_OK)
     {
-        return -1;
+        ESP_LOGE(TAG,
+                 "SQLite post-migration integrity check failed: rc=%d. Database is preserved.",
+                 rc);
+        goto fail;
     }
 
-    rc = sqlite3_step(stmt);
-    if (rc == SQLITE_ROW)
+    int cache_count = 0;
+    if (get_record_count_locked(&cache_count) == 0)
     {
-        *overwrite_seq_no = sqlite3_column_int(stmt, 0);
-        sqlite3_finalize(stmt);
-        return 0;
+        ESP_LOGI(TAG, "SQLite cache record count=%d", cache_count);
     }
 
-    sqlite3_finalize(stmt);
-    return -1;
-}
+    print_sqlite_pragmas(s_db);
+    print_db_files();
 
-static int bind_structured_record(sqlite3_stmt *stmt,
-                                  int seq_no,
-                                  const char *sn,
-                                  int current_step,
-                                  int timestamp,
-                                  bool pushed,
-                                  const char *value_data,
-                                  int created_at)
-{
-    if (stmt == NULL || sn == NULL || value_data == NULL)
+    s_db_ready = true;
+    ESP_LOGI(TAG, "Database initialized successfully: %s", DB_FILE_PATH);
+    return SQLITE_OK;
+
+fail:
+    s_db_ready = false;
+    if (s_db != NULL)
     {
-        return SQLITE_MISUSE;
+        int close_rc = sqlite3_close(s_db);
+        if (close_rc == SQLITE_OK)
+        {
+            s_db = NULL;
+        }
+        else
+        {
+            ESP_LOGE(TAG, "sqlite3_close after init failure returned rc=%d", close_rc);
+        }
     }
-
-    sqlite3_bind_int(stmt, 1, seq_no);
-    sqlite3_bind_text(stmt, 2, sn, -1, SQLITE_TRANSIENT);
-    sqlite3_bind_int(stmt, 3, current_step);
-    sqlite3_bind_int(stmt, 4, timestamp);
-    sqlite3_bind_int(stmt, 5, pushed ? 1 : 0);
-    sqlite3_bind_text(stmt, 6, value_data, -1, SQLITE_TRANSIENT);
-    sqlite3_bind_int(stmt, 7, created_at);
-
-    return SQLITE_OK;
+    return rc;
 }
 
+/* -------------------------------------------------------------------------- */
+/* Value JSON校验与容量控制                                                     */
+/* -------------------------------------------------------------------------- */
+
 static bool is_valid_value_data(const char *value_data)
 {
     if (value_data == NULL || value_data[0] == '\0')
@@ -859,6 +808,7 @@
 
         cJSON *name = cJSON_GetObjectItemCaseSensitive(group, "Name");
         cJSON *data = cJSON_GetObjectItemCaseSensitive(group, "Data");
+
         if (!cJSON_IsString(name) || name->valuestring == NULL || name->valuestring[0] == '\0' ||
             !cJSON_IsArray(data))
         {
@@ -877,6 +827,7 @@
 
             cJSON *item_name = cJSON_GetObjectItemCaseSensitive(item, "name");
             cJSON *item_value = cJSON_GetObjectItemCaseSensitive(item, "value");
+
             if (!cJSON_IsString(item_name) || item_name->valuestring == NULL || item_name->valuestring[0] == '\0' ||
                 !(cJSON_IsString(item_value) || cJSON_IsNumber(item_value)))
             {
@@ -894,6 +845,7 @@
 {
     uint64_t total_bytes = 0;
     uint64_t free_bytes = 0;
+
     if (esp_vfs_fat_info(FATFS_BASE_PATH, &total_bytes, &free_bytes) != ESP_OK)
     {
         return false;
@@ -902,16 +854,94 @@
     return free_bytes < DB_MIN_FREE_BYTES;
 }
 
-/* =========================================================
- * 结构化插入/更新一条记录
- *
- * 规则：
- *   1. 如果 seq_no 已存在，直接更新该 seq_no。
- *   2. 如果 seq_no 不存在且表未满，插入新记录。
- *   3. 如果 seq_no 不存在且表已满，覆盖最旧记录；优先覆盖 pushed=1 的旧记录。
- *
- * 注意：没有自增 local_id，seq_no 就是唯一定位字段。
- * ========================================================= */
+static int get_existing_local_id_locked(const char *sn, int seq_no, sqlite3_int64 *local_id)
+{
+    *local_id = -1;
+
+    sqlite3_stmt *stmt = NULL;
+    const char *sql =
+        "SELECT local_id FROM " SQLITE_CACHE_TABLE_NAME " "
+        "WHERE sn=? AND seq_no=? LIMIT 1;";
+
+    int rc = sqlite3_prepare_v2(s_db, sql, -1, &stmt, NULL);
+    if (rc != SQLITE_OK)
+    {
+        return rc;
+    }
+
+    sqlite3_bind_text(stmt, 1, sn, -1, SQLITE_TRANSIENT);
+    sqlite3_bind_int(stmt, 2, seq_no);
+
+    rc = sqlite3_step(stmt);
+    if (rc == SQLITE_ROW)
+    {
+        *local_id = sqlite3_column_int64(stmt, 0);
+        rc = SQLITE_OK;
+    }
+    else if (rc == SQLITE_DONE)
+    {
+        rc = SQLITE_NOTFOUND;
+    }
+
+    sqlite3_finalize(stmt);
+    return rc;
+}
+
+static int get_overwrite_local_id_locked(sqlite3_int64 *local_id)
+{
+    *local_id = -1;
+
+    /* pushed=1优先淘汰；同状态下local_id越小越旧。 */
+    const char *sql =
+        "SELECT local_id FROM " SQLITE_CACHE_TABLE_NAME " "
+        "ORDER BY pushed DESC, local_id ASC LIMIT 1;";
+
+    sqlite3_stmt *stmt = NULL;
+    int rc = sqlite3_prepare_v2(s_db, sql, -1, &stmt, NULL);
+    if (rc != SQLITE_OK)
+    {
+        return rc;
+    }
+
+    rc = sqlite3_step(stmt);
+    if (rc == SQLITE_ROW)
+    {
+        *local_id = sqlite3_column_int64(stmt, 0);
+        rc = SQLITE_OK;
+    }
+    else if (rc == SQLITE_DONE)
+    {
+        rc = SQLITE_NOTFOUND;
+    }
+
+    sqlite3_finalize(stmt);
+    return rc;
+}
+
+static int delete_local_id_locked(sqlite3_int64 local_id)
+{
+    sqlite3_stmt *stmt = NULL;
+    int rc = sqlite3_prepare_v2(s_db,
+                                "DELETE FROM " SQLITE_CACHE_TABLE_NAME " WHERE local_id=?;",
+                                -1,
+                                &stmt,
+                                NULL);
+    if (rc != SQLITE_OK)
+    {
+        return rc;
+    }
+
+    sqlite3_bind_int64(stmt, 1, local_id);
+    rc = sqlite3_step(stmt);
+    sqlite3_finalize(stmt);
+
+    return (rc == SQLITE_DONE) ? SQLITE_OK : rc;
+}
+
+/* -------------------------------------------------------------------------- */
+/* 保存                                                                        */
+/* -------------------------------------------------------------------------- */
+
 int InsertStructuredRecord(int seq_no,
                            const char *sn,
                            int current_step,
@@ -919,7 +949,7 @@
                            bool pushed,
                            const char *value_data)
 {
-    if (sn == NULL  || value_data == NULL)
+    if (seq_no < 0 || sn == NULL || sn[0] == '\0' || value_data == NULL)
     {
         ESP_LOGE(TAG, "InsertStructuredRecord invalid parameter");
         return -1;
@@ -927,154 +957,151 @@
 
     if (strlen(sn) >= DB_SN_MAX_LEN)
     {
-        ESP_LOGE(TAG, "SN length invalid: %u bytes, max=%d",
-                 (unsigned int)strlen(sn),
-                 DB_SN_MAX_LEN - 1);
+        ESP_LOGE(TAG, "SN length invalid: %u bytes", (unsigned)strlen(sn));
         return -1;
     }
 
     if (!is_valid_value_data(value_data))
     {
-        ESP_LOGE(TAG, "Value JSON invalid or too long, len=%u", value_data ? (unsigned)strlen(value_data) : 0U);
+        ESP_LOGE(TAG, "Value JSON invalid or too long, len=%u",
+                 (unsigned)(value_data ? strlen(value_data) : 0));
         return -1;
     }
 
-    if (db1_name[0] == '\0')
+    if (!sqlite_lock())
     {
-        snprintf(db1_name, sizeof(db1_name), "%s", DB_FILE_PATH);
+        return -1;
     }
 
-    sqlite3 *db = NULL;
-    sqlite3_stmt *stmt = NULL;
-    int rc = SQLITE_OK;
-    int record_count = 0;
-    int exists = 0;
-    int overwrite_seq_no = -1;
-    int created_at = sqlite_now_seconds();
-
-    sqlite3_initialize();
-
-    if (db_open(db1_name, &db) != SQLITE_OK)
+    if (!sqlite_ready_locked())
     {
-        ESP_LOGE(TAG, "Failed to open database %s", db1_name);
+        sqlite_unlock();
         return -1;
     }
 
-    apply_sqlite_pragmas(db);
-
-    rc = db_exec(db, "BEGIN IMMEDIATE TRANSACTION;");
+    int rc = db_exec(s_db, "BEGIN IMMEDIATE TRANSACTION;");
     if (rc != SQLITE_OK)
     {
-        sqlite3_close(db);
+        ESP_LOGE(TAG, "BEGIN transaction failed: rc=%d, errmsg=%s", rc, sqlite3_errmsg(s_db));
+        sqlite_unlock();
         return -1;
     }
 
-    if (get_existing_seq_no(db, seq_no, &exists) != 0)
+    sqlite3_stmt *stmt = NULL;
+    sqlite3_int64 existing_local_id = -1;
+    int existing_rc = get_existing_local_id_locked(sn, seq_no, &existing_local_id);
+
+    if (existing_rc != SQLITE_OK && existing_rc != SQLITE_NOTFOUND)
     {
-        ESP_LOGE(TAG, "check existing seq_no failed: %s", sqlite3_errmsg(db));
+        ESP_LOGE(TAG, "Check existing record failed: rc=%d, errmsg=%s",
+                 existing_rc, sqlite3_errmsg(s_db));
         goto rollback;
     }
 
-    if (!exists)
+    if (existing_rc == SQLITE_NOTFOUND)
     {
-        if (get_record_count(db, &record_count) != 0)
+        int record_count = 0;
+        if (get_record_count_locked(&record_count) != 0)
         {
-            ESP_LOGE(TAG, "get record count failed: %s", sqlite3_errmsg(db));
+            ESP_LOGE(TAG, "Get record count failed");
             goto rollback;
         }
 
-        bool low_free_space = sqlite_storage_low_on_free_space();
-        if (record_count > 0 &&
-            (record_count >= MAX_RECORDS_COUNT || low_free_space))
+        bool low_space = sqlite_storage_low_on_free_space();
+        if (record_count > 0 && (record_count >= MAX_RECORDS_COUNT || low_space))
         {
-            if (get_overwrite_seq_no(db, &overwrite_seq_no) != 0)
+            sqlite3_int64 overwrite_local_id = -1;
+            rc = get_overwrite_local_id_locked(&overwrite_local_id);
+            if (rc != SQLITE_OK || overwrite_local_id < 0)
             {
-                ESP_LOGE(TAG, "get overwrite target failed: %s", sqlite3_errmsg(db));
+                ESP_LOGE(TAG, "No record available for cache eviction");
                 goto rollback;
             }
 
-            if (low_free_space)
+            rc = delete_local_id_locked(overwrite_local_id);
+            if (rc != SQLITE_OK)
             {
-                ESP_LOGW(TAG, "SQLite storage free space below reserve; overwriting cached record");
+                ESP_LOGE(TAG, "Delete old cache record failed: rc=%d", rc);
+                goto rollback;
             }
+
+            ESP_LOGW(TAG,
+                     "SQLite cache eviction: local_id=%lld reason=%s",
+                     (long long)overwrite_local_id,
+                     low_space ? "low_free_space" : "max_record_count");
         }
     }
 
-    if (exists)
-    {
-        const char *update_sql =
-            "UPDATE " SQLITE_CACHE_TABLE_NAME " "
-            "SET sn = ?, current_step = ?, timestamp = ?, pushed = ?, value_data = ?, created_at = ? "
-            "WHERE seq_no = ?;";
-
-        rc = sqlite3_prepare_v2(db, update_sql, -1, &stmt, NULL);
-        if (rc != SQLITE_OK)
-        {
-            ESP_LOGE(TAG, "prepare update failed: %s", sqlite3_errmsg(db));
-            goto rollback;
-        }
+    int created_at = sqlite_now_seconds();
 
-        sqlite3_bind_text(stmt, 1, sn, -1, SQLITE_TRANSIENT);
-        sqlite3_bind_int(stmt, 2, current_step);
-        sqlite3_bind_int(stmt, 3, timestamp);
-        sqlite3_bind_int(stmt, 4, pushed ? 1 : 0);
-        sqlite3_bind_text(stmt, 5, value_data, -1, SQLITE_TRANSIENT);
-        sqlite3_bind_int(stmt, 6, created_at);
-        sqlite3_bind_int(stmt, 7, seq_no);
-    }
-    else if (overwrite_seq_no >= 0)
+    if (existing_rc == SQLITE_OK)
     {
-        const char *overwrite_sql =
+        const char *sql =
             "UPDATE " SQLITE_CACHE_TABLE_NAME " "
-            "SET seq_no = ?, sn = ?, current_step = ?, timestamp = ?, pushed = ?, value_data = ?, created_at = ? "
-            "WHERE seq_no = ?;";
+            "SET current_step=?, timestamp=?, pushed=?, value_data=?, created_at=? "
+            "WHERE local_id=?;";
 
-        rc = sqlite3_prepare_v2(db, overwrite_sql, -1, &stmt, NULL);
+        rc = sqlite3_prepare_v2(s_db, sql, -1, &stmt, NULL);
         if (rc != SQLITE_OK)
         {
-            ESP_LOGE(TAG, "prepare overwrite failed: %s", sqlite3_errmsg(db));
+            ESP_LOGE(TAG, "Prepare update failed: %s", sqlite3_errmsg(s_db));
             goto rollback;
         }
 
-        bind_structured_record(stmt, seq_no, sn, current_step, timestamp, pushed, value_data, created_at);
-        sqlite3_bind_int(stmt, 8, overwrite_seq_no);
+        sqlite3_bind_int(stmt, 1, current_step);
+        sqlite3_bind_int(stmt, 2, timestamp);
+        sqlite3_bind_int(stmt, 3, pushed ? 1 : 0);
+        sqlite3_bind_text(stmt, 4, value_data, -1, SQLITE_TRANSIENT);
+        sqlite3_bind_int(stmt, 5, created_at);
+        sqlite3_bind_int64(stmt, 6, existing_local_id);
     }
     else
     {
-        const char *insert_sql =
+        const char *sql =
             "INSERT INTO " SQLITE_CACHE_TABLE_NAME " "
             "(seq_no, sn, current_step, timestamp, pushed, value_data, created_at) "
             "VALUES (?, ?, ?, ?, ?, ?, ?);";
 
-        rc = sqlite3_prepare_v2(db, insert_sql, -1, &stmt, NULL);
+        rc = sqlite3_prepare_v2(s_db, sql, -1, &stmt, NULL);
         if (rc != SQLITE_OK)
         {
-            ESP_LOGE(TAG, "prepare insert failed: %s", sqlite3_errmsg(db));
+            ESP_LOGE(TAG, "Prepare insert failed: %s", sqlite3_errmsg(s_db));
             goto rollback;
         }
 
-        bind_structured_record(stmt, seq_no, sn, current_step, timestamp, pushed, value_data, created_at);
+        sqlite3_bind_int(stmt, 1, seq_no);
+        sqlite3_bind_text(stmt, 2, sn, -1, SQLITE_TRANSIENT);
+        sqlite3_bind_int(stmt, 3, current_step);
+        sqlite3_bind_int(stmt, 4, timestamp);
+        sqlite3_bind_int(stmt, 5, pushed ? 1 : 0);
+        sqlite3_bind_text(stmt, 6, value_data, -1, SQLITE_TRANSIENT);
+        sqlite3_bind_int(stmt, 7, created_at);
     }
 
     rc = sqlite3_step(stmt);
     if (rc != SQLITE_DONE)
     {
-        ESP_LOGE(TAG, "insert/update step failed: rc=%d, errmsg=%s", rc, sqlite3_errmsg(db));
+        ESP_LOGE(TAG, "Insert/update step failed: rc=%d, ext=%d, errmsg=%s",
+                 rc,
+                 sqlite3_extended_errcode(s_db),
+                 sqlite3_errmsg(s_db));
         goto rollback;
     }
 
     sqlite3_finalize(stmt);
     stmt = NULL;
 
-    rc = db_exec(db, "COMMIT;");
+    rc = db_exec(s_db, "COMMIT;");
     if (rc != SQLITE_OK)
     {
-        sqlite3_close(db);
+        ESP_LOGE(TAG, "COMMIT failed: rc=%d, errmsg=%s", rc, sqlite3_errmsg(s_db));
+        (void)db_exec(s_db, "ROLLBACK;");
+        sqlite_unlock();
         return -1;
     }
 
     ESP_LOGI(TAG,
-             "structured record saved: seq_no=%d, sn=%s, step=%d, timestamp=%d, pushed=%d, value_len=%u",
+             "structured record committed: seq_no=%d, sn=%s, step=%d, timestamp=%d, pushed=%d, value_len=%u",
              seq_no,
              sn,
              current_step,
@@ -1082,7 +1109,7 @@
              pushed ? 1 : 0,
              (unsigned)strlen(value_data));
 
-    sqlite3_close(db);
+    sqlite_unlock();
     return 0;
 
 rollback:
@@ -1090,8 +1117,8 @@
     {
         sqlite3_finalize(stmt);
     }
-    db_exec(db, "ROLLBACK;");
-    sqlite3_close(db);
+    (void)db_exec(s_db, "ROLLBACK;");
+    sqlite_unlock();
     return -1;
 }
 
@@ -1105,7 +1132,6 @@
 {
     if (values == NULL || value_count <= 0 || value_count > DB_VALUE_MAX_COUNT)
     {
-        ESP_LOGE(TAG, "InsertDataReportValues invalid value array");
         return -1;
     }
 
@@ -1149,59 +1175,38 @@
         return -1;
     }
 
-    int ret = InsertStructuredRecord(seq_no,
-                                     sn,
-                                     current_step,
-                                     timestamp,
-                                     pushed,
-                                     value_json);
+    int ret = InsertStructuredRecord(seq_no, sn, current_step, timestamp, pushed, value_json);
     free(value_json);
     return ret;
 }
 
+/* -------------------------------------------------------------------------- */
+/* 更新                                                                        */
+/* -------------------------------------------------------------------------- */
+
 int UpdateRecordPushStateBySNAndID(const char *sn, int seq_no, bool pushed)
 {
     if (sn == NULL || sn[0] == '\0' || seq_no < 0)
     {
-        ESP_LOGW(TAG, "Invalid parameter: sn=%s, seq_no=%d",
-                 sn ? sn : "NULL",
-                 seq_no);
-        return -1;
-    }
-
-    sqlite3 *db = NULL;
-    sqlite3_stmt *stmt = NULL;
-    int rc;
-
-    rc = db_open(DB_FILE_PATH, &db);
-    if (rc != SQLITE_OK)
-    {
-        ESP_LOGE(TAG, "Cannot open database: %s", db ? sqlite3_errmsg(db) : "unknown");
-        if (db)
-        {
-            sqlite3_close(db);
-        }
         return -1;
     }
 
-    if (apply_sqlite_pragmas(db) != SQLITE_OK)
+    if (!sqlite_lock()) return -1;
+    if (!sqlite_ready_locked())
     {
-        ESP_LOGE(TAG, "Apply SQLite pragmas failed before push-state update");
-        sqlite3_close(db);
+        sqlite_unlock();
         return -1;
     }
 
+    sqlite3_stmt *stmt = NULL;
     const char *sql =
-        "UPDATE " SQLITE_CACHE_TABLE_NAME " "
-        "SET pushed = ? "
-        "WHERE sn = ? AND seq_no = ?;";
+        "UPDATE " SQLITE_CACHE_TABLE_NAME " SET pushed=? WHERE sn=? AND seq_no=?;";
 
-    rc = sqlite3_prepare_v2(db, sql, -1, &stmt, NULL);
+    int rc = sqlite3_prepare_v2(s_db, sql, -1, &stmt, NULL);
     if (rc != SQLITE_OK)
     {
-        ESP_LOGE(TAG, "Prepare update pushed by SN and ID failed: %s",
-                 sqlite3_errmsg(db));
-        sqlite3_close(db);
+        ESP_LOGE(TAG, "Prepare pushed update failed: %s", sqlite3_errmsg(s_db));
+        sqlite_unlock();
         return -1;
     }
 
@@ -1210,387 +1215,216 @@
     sqlite3_bind_int(stmt, 3, seq_no);
 
     rc = sqlite3_step(stmt);
-    if (rc != SQLITE_DONE)
-    {
-        ESP_LOGE(TAG, "Update pushed by SN and ID failed: %s",
-                 sqlite3_errmsg(db));
-        sqlite3_finalize(stmt);
-        sqlite3_close(db);
-        return -1;
-    }
-
-    int changed = sqlite3_changes(db);
-
+    int changed = (rc == SQLITE_DONE) ? sqlite3_changes(s_db) : 0;
     sqlite3_finalize(stmt);
-    sqlite3_close(db);
 
-    if (changed <= 0)
+    if (rc != SQLITE_DONE || changed <= 0)
     {
-        ESP_LOGW(TAG, "No record updated: SN=%s, seq_no=%d, pushed=%d",
-                 sn,
-                 seq_no,
-                 pushed ? 1 : 0);
+        ESP_LOGW(TAG, "Push-state update failed/not found: SN=%s seq_no=%d rc=%d",
+                 sn, seq_no, rc);
+        sqlite_unlock();
         return -1;
     }
 
-    ESP_LOGI(TAG, "Update pushed success: SN=%s, seq_no=%d, pushed=%d",
-             sn,
-             seq_no,
-             pushed ? 1 : 0);
-
+    ESP_LOGI(TAG, "Push-state updated: SN=%s seq_no=%d pushed=%d",
+             sn, seq_no, pushed ? 1 : 0);
+    sqlite_unlock();
     return 0;
 }
 
-static int serialize_value_array_for_storage(cJSON *value_item,
-                                             char *out_value_data,
-                                             size_t out_size)
+int UpdateRecordPushState(int seq_no, bool pushed)
 {
-    if (!cJSON_IsArray(value_item) || out_value_data == NULL || out_size == 0)
+    if (seq_no < 0) return -1;
+    if (!sqlite_lock()) return -1;
+    if (!sqlite_ready_locked())
     {
+        sqlite_unlock();
         return -1;
     }
 
-    cJSON *storage_array = NULL;
-    cJSON *first = cJSON_GetArrayItem(value_item, 0);
-
-    if (first == NULL || cJSON_IsObject(first))
-    {
-        storage_array = cJSON_Duplicate(value_item, true);
-    }
-    else
-    {
-        /* 兼容旧的 Value:["74.00","52.66"] 形式。 */
-        storage_array = cJSON_CreateArray();
-        cJSON *group = cJSON_CreateObject();
-        cJSON *data = cJSON_CreateArray();
-        if (storage_array == NULL || group == NULL || data == NULL)
-        {
-            cJSON_Delete(storage_array);
-            cJSON_Delete(group);
-            cJSON_Delete(data);
-            return -1;
-        }
-
-        cJSON_AddStringToObject(group, "Name", "Legacy");
-        cJSON_AddItemToObject(group, "Data", data);
-        cJSON_AddItemToArray(storage_array, group);
-
-        int count = cJSON_GetArraySize(value_item);
-        for (int i = 0; i < count; i++)
-        {
-            cJSON *src = cJSON_GetArrayItem(value_item, i);
-            cJSON *item = cJSON_CreateObject();
-            if (item == NULL)
-            {
-                cJSON_Delete(storage_array);
-                return -1;
-            }
-
-            char name[24];
-            char value_string[32];
-            snprintf(name, sizeof(name), "Value%d", i);
-
-            if (cJSON_IsString(src) && src->valuestring != NULL)
-            {
-                snprintf(value_string, sizeof(value_string), "%s", src->valuestring);
-            }
-            else if (cJSON_IsNumber(src))
-            {
-                snprintf(value_string, sizeof(value_string), "%.2f", src->valuedouble);
-            }
-            else
-            {
-                cJSON_Delete(item);
-                cJSON_Delete(storage_array);
-                return -1;
-            }
-
-            cJSON_AddStringToObject(item, "name", name);
-            cJSON_AddStringToObject(item, "value", value_string);
-            cJSON_AddItemToArray(data, item);
-        }
-    }
-
-    if (storage_array == NULL)
-    {
-        return -1;
-    }
+    /* legacy：若多个SN拥有相同seq_no，只更新最近插入的一条。 */
+    const char *sql =
+        "UPDATE " SQLITE_CACHE_TABLE_NAME " SET pushed=? "
+        "WHERE local_id=(SELECT local_id FROM " SQLITE_CACHE_TABLE_NAME " "
+        "WHERE seq_no=? ORDER BY local_id DESC LIMIT 1);";
 
-    char *json = cJSON_PrintUnformatted(storage_array);
-    cJSON_Delete(storage_array);
-    if (json == NULL)
+    sqlite3_stmt *stmt = NULL;
+    int rc = sqlite3_prepare_v2(s_db, sql, -1, &stmt, NULL);
+    if (rc != SQLITE_OK)
     {
+        sqlite_unlock();
         return -1;
     }
 
-    size_t len = strlen(json);
-    if (len >= out_size)
-    {
-        free(json);
-        return -1;
-    }
+    sqlite3_bind_int(stmt, 1, pushed ? 1 : 0);
+    sqlite3_bind_int(stmt, 2, seq_no);
+    rc = sqlite3_step(stmt);
+    int changed = (rc == SQLITE_DONE) ? sqlite3_changes(s_db) : 0;
+    sqlite3_finalize(stmt);
+    sqlite_unlock();
 
-    memcpy(out_value_data, json, len + 1);
-    free(json);
-    return 0;
+    return (rc == SQLITE_DONE && changed > 0) ? 0 : -1;
 }
 
-/*
- * 兼容接口：既接受完整 {Seq,Data:{...}} 新格式，也兼容旧的扁平字段格式。
- * SQLite 最终只保存结构化 Value 数组 JSON。
- */
-int InsertJsonRecord(int seq_no, const char *sn, bool pushed, const char *json_data)
-{
-    if (json_data == NULL)
-    {
-        ESP_LOGE(TAG, "InsertJsonRecord json_data is NULL");
-        return -1;
-    }
+/* -------------------------------------------------------------------------- */
+/* 查询                                                                        */
+/* -------------------------------------------------------------------------- */
 
-    cJSON *root = cJSON_Parse(json_data);
-    if (root == NULL)
+static int query_one_locked(const char *sql,
+                            const char *sn,
+                            int seq_no,
+                            int pushed_filter,
+                            QueryResult *out_result)
+{
+    sqlite3_stmt *stmt = NULL;
+    int rc = sqlite3_prepare_v2(s_db, sql, -1, &stmt, NULL);
+    if (rc != SQLITE_OK)
     {
-        ESP_LOGE(TAG, "InsertJsonRecord parse json failed");
+        ESP_LOGE(TAG, "Prepare query failed: %s", sqlite3_errmsg(s_db));
         return -1;
     }
 
-    cJSON *payload = cJSON_GetObjectItemCaseSensitive(root, "Data");
-    if (!cJSON_IsObject(payload))
+    int bind_index = 1;
+    if (sn != NULL)
     {
-        payload = root;
+        sqlite3_bind_text(stmt, bind_index++, sn, -1, SQLITE_TRANSIENT);
     }
-
-    const char *final_sn = sn;
-    int final_seq_no = seq_no;
-    int final_timestamp = 0;
-    int final_step = 0;
-    char value_data[DB_VALUE_DATA_MAX_LEN + 1] = {0};
-
-    cJSON *sn_item = cJSON_GetObjectItemCaseSensitive(payload, "SN");
-    cJSON *id_item = cJSON_GetObjectItemCaseSensitive(payload, "IDNUM");
-    cJSON *timestamp_item = cJSON_GetObjectItemCaseSensitive(payload, "TIMESTAMP");
-    cJSON *step_item = cJSON_GetObjectItemCaseSensitive(payload, "CurrentStep");
-    cJSON *value_item = cJSON_GetObjectItemCaseSensitive(payload, "Value");
-
-    if ((final_sn == NULL || final_sn[0] == '\0') && cJSON_IsString(sn_item) && sn_item->valuestring != NULL)
-    {
-        final_sn = sn_item->valuestring;
-    }
-
-    if (final_seq_no <= 0 && cJSON_IsNumber(id_item))
-    {
-        final_seq_no = id_item->valueint;
-    }
-
-    if (cJSON_IsNumber(timestamp_item))
+    if (seq_no >= 0)
     {
-        final_timestamp = timestamp_item->valueint;
+        sqlite3_bind_int(stmt, bind_index++, seq_no);
     }
-
-    if (cJSON_IsNumber(step_item))
+    if (pushed_filter >= 0)
     {
-        final_step = step_item->valueint;
+        sqlite3_bind_int(stmt, bind_index++, pushed_filter);
     }
 
-    if (serialize_value_array_for_storage(value_item, value_data, sizeof(value_data)) != 0)
+    rc = sqlite3_step(stmt);
+    if (rc == SQLITE_ROW)
     {
-        ESP_LOGE(TAG, "InsertJsonRecord parse Value array failed");
-        cJSON_Delete(root);
-        return -1;
+        fill_query_result_from_stmt(stmt, out_result);
+        sqlite3_finalize(stmt);
+        return 0;
     }
 
-    if (final_sn == NULL || final_sn[0] == '\0' || final_seq_no < 0 || final_timestamp <= 0)
+    if (rc != SQLITE_DONE)
     {
-        ESP_LOGE(TAG, "InsertJsonRecord missing SN/IDNUM/TIMESTAMP");
-        cJSON_Delete(root);
-        return -1;
+        ESP_LOGE(TAG, "Query step failed: rc=%d, errmsg=%s", rc, sqlite3_errmsg(s_db));
     }
 
-    int ret = InsertStructuredRecord(final_seq_no,
-                                     final_sn,
-                                     final_step,
-                                     final_timestamp,
-                                     pushed,
-                                     value_data);
-
-    cJSON_Delete(root);
-    return ret;
+    sqlite3_finalize(stmt);
+    return -1;
 }
 
-/// @brief 按序列号查询结构化缓存记录，并自动重构 json_data
-/// @return 0: 查到；-1: 未查到或失败
-int QueryStructuredRecordBySeq(int seq_no, QueryResult *out_result)
+int QueryStructuredRecordLatestBySN(const char *sn, QueryResult *out_result)
 {
-    if (out_result == NULL)
+    if (sn == NULL || sn[0] == '\0' || out_result == NULL)
     {
         return -1;
     }
 
     memset(out_result, 0, sizeof(QueryResult));
 
-    if (db1_name[0] == '\0')
-    {
-        snprintf(db1_name, sizeof(db1_name), "%s", DB_FILE_PATH);
-    }
-
-    sqlite3 *db = NULL;
-    sqlite3_stmt *stmt = NULL;
-
-    int rc = db_open(db1_name, &db);
-    if (rc != SQLITE_OK)
+    if (!sqlite_lock()) return -1;
+    if (!sqlite_ready_locked())
     {
-        ESP_LOGE(TAG, "Failed to open database %s", db1_name);
+        sqlite_unlock();
         return -1;
     }
 
-    apply_sqlite_pragmas(db);
-
     const char *sql =
         "SELECT seq_no, sn, current_step, timestamp, pushed, value_data, created_at "
         "FROM " SQLITE_CACHE_TABLE_NAME " "
-        "WHERE seq_no = ? "
-        "LIMIT 1;";
+        "WHERE sn=? ORDER BY seq_no DESC, local_id DESC LIMIT 1;";
 
-    rc = sqlite3_prepare_v2(db, sql, -1, &stmt, NULL);
-    if (rc != SQLITE_OK)
+    int ret = query_one_locked(sql, sn, -1, -1, out_result);
+    if (ret == 0)
     {
-        ESP_LOGE(TAG, "prepare query failed: %s", sqlite3_errmsg(db));
-        sqlite3_close(db);
-        return -1;
-    }
-
-    sqlite3_bind_int(stmt, 1, seq_no);
-
-    rc = sqlite3_step(stmt);
-    if (rc == SQLITE_ROW)
-    {
-        fill_query_result_from_stmt(stmt, out_result);
-        ESP_LOGI(TAG, "Query success: seq_no=%d, pushed=%d, value_data=%s",
-                 out_result->seq_no,
-                 out_result->pushed ? 1 : 0,
-                 out_result->value_data);
-        sqlite3_finalize(stmt);
-        sqlite3_close(db);
-        return 0;
-    }
-
-    if (rc == SQLITE_DONE)
-    {
-        ESP_LOGW(TAG, "No cached record found: seq_no=%d", seq_no);
+        ESP_LOGI(TAG, "Latest record found: SN=%s last_seq=%d pushed=%d",
+                 sn, out_result->seq_no, out_result->pushed ? 1 : 0);
     }
     else
     {
-        ESP_LOGE(TAG, "query step failed: rc=%d, errmsg=%s", rc, sqlite3_errmsg(db));
+        ESP_LOGI(TAG, "No cached record for SN=%s", sn);
     }
 
-    sqlite3_finalize(stmt);
-    sqlite3_close(db);
-    return -1;
-}
-
-int QueryJsonRecordBySeq(int seq_no, QueryResult *out_result)
-{
-    return QueryStructuredRecordBySeq(seq_no, out_result);
+    sqlite_unlock();
+    return ret;
 }
 
-int QueryStructuredRecordLatestBySN(const char *sn, QueryResult *out_result)
+int QueryStructuredRecordBySeq(int seq_no, QueryResult *out_result)
 {
-    if (sn == NULL || out_result == NULL || sn[0] == '\0')
-    {
-        ESP_LOGW(TAG, "Invalid parameter: sn or out_result is NULL");
-        return -1;
-    }
-
+    if (seq_no < 0 || out_result == NULL) return -1;
     memset(out_result, 0, sizeof(QueryResult));
 
-    sqlite3 *db = NULL;
-    sqlite3_stmt *stmt = NULL;
-    if (db_open(DB_FILE_PATH, &db) != SQLITE_OK)
+    if (!sqlite_lock()) return -1;
+    if (!sqlite_ready_locked())
     {
-        ESP_LOGE(TAG, "Cannot open database");
+        sqlite_unlock();
         return -1;
     }
 
-    apply_sqlite_pragmas(db);
-
+    /* legacy：跨SN时返回最近插入的同seq_no记录。 */
     const char *sql =
         "SELECT seq_no, sn, current_step, timestamp, pushed, value_data, created_at "
         "FROM " SQLITE_CACHE_TABLE_NAME " "
-        "WHERE sn = ? "
-        "ORDER BY timestamp DESC, seq_no DESC "
-        "LIMIT 1;";
-
-    int rc = sqlite3_prepare_v2(db, sql, -1, &stmt, NULL);
-    if (rc != SQLITE_OK)
-    {
-        ESP_LOGE(TAG, "Prepare latest SN query failed: %s", sqlite3_errmsg(db));
-        sqlite3_close(db);
-        return -1;
-    }
+        "WHERE seq_no=? ORDER BY local_id DESC LIMIT 1;";
 
-    sqlite3_bind_text(stmt, 1, sn, -1, SQLITE_TRANSIENT);
-    rc = sqlite3_step(stmt);
-    if (rc == SQLITE_ROW)
-    {
-        fill_query_result_from_stmt(stmt, out_result);
-        sqlite3_finalize(stmt);
-        sqlite3_close(db);
-        return 0;
-    }
+    int ret = query_one_locked(sql, NULL, seq_no, -1, out_result);
+    sqlite_unlock();
+    return ret;
+}
 
-    ESP_LOGW(TAG, "No cached record found for SN=%s", sn);
-    sqlite3_finalize(stmt);
-    sqlite3_close(db);
-    return -1;
+int QueryJsonRecordBySeq(int seq_no, QueryResult *out_result)
+{
+    return QueryStructuredRecordBySeq(seq_no, out_result);
 }
 
 void query_db1_latest_by_sn_to_global(const char *sn)
 {
-    memset(&g_db1_result, 0, sizeof(QueryResult));
+    memset(&g_db1_result, 0, sizeof(g_db1_result));
     (void)QueryStructuredRecordLatestBySN(sn, &g_db1_result);
 }
 
+void query_db1_to_global(int target_id)
+{
+    memset(&g_db1_result, 0, sizeof(g_db1_result));
+    (void)QueryStructuredRecordBySeq(target_id, &g_db1_result);
+}
+
 int QueryStructuredRecordsBySNAndPushState(const char *sn,
                                            bool pushed,
                                            QueryResult *out_array,
                                            int max_count,
                                            int *out_count)
 {
-    if (out_count != NULL)
-    {
-        *out_count = 0;
-    }
+    if (out_count != NULL) *out_count = 0;
 
     if (sn == NULL || sn[0] == '\0' || out_array == NULL || max_count <= 0 || out_count == NULL)
     {
-        ESP_LOGW(TAG, "Invalid parameter in QueryStructuredRecordsBySNAndPushState");
         return -1;
     }
 
     memset(out_array, 0, sizeof(QueryResult) * max_count);
 
-    sqlite3 *db = NULL;
-    sqlite3_stmt *stmt = NULL;
-    if (db_open(DB_FILE_PATH, &db) != SQLITE_OK)
+    if (!sqlite_lock()) return -1;
+    if (!sqlite_ready_locked())
     {
-        ESP_LOGE(TAG, "Cannot open database");
+        sqlite_unlock();
         return -1;
     }
 
-    apply_sqlite_pragmas(db);
-
     const char *sql =
         "SELECT seq_no, sn, current_step, timestamp, pushed, value_data, created_at "
         "FROM " SQLITE_CACHE_TABLE_NAME " "
-        "WHERE sn = ? AND pushed = ? "
-        "ORDER BY timestamp DESC, seq_no DESC "
-        "LIMIT ?;";
+        "WHERE sn=? AND pushed=? "
+        "ORDER BY seq_no ASC, local_id ASC LIMIT ?;";
 
-    int rc = sqlite3_prepare_v2(db, sql, -1, &stmt, NULL);
+    sqlite3_stmt *stmt = NULL;
+    int rc = sqlite3_prepare_v2(s_db, sql, -1, &stmt, NULL);
     if (rc != SQLITE_OK)
     {
-        ESP_LOGE(TAG, "Prepare query by SN and pushed failed: %s", sqlite3_errmsg(db));
-        sqlite3_close(db);
+        ESP_LOGE(TAG, "Prepare SN/pushed query failed: %s", sqlite3_errmsg(s_db));
+        sqlite_unlock();
         return -1;
     }
 
@@ -1601,205 +1435,296 @@
     int count = 0;
     while (count < max_count && (rc = sqlite3_step(stmt)) == SQLITE_ROW)
     {
-        fill_query_result_from_stmt(stmt, &out_array[count]);
-        count++;
-    }
-
-    if (rc != SQLITE_DONE)
-    {
-        ESP_LOGE(TAG, "Step query by SN and pushed failed: %s", sqlite3_errmsg(db));
-        sqlite3_finalize(stmt);
-        sqlite3_close(db);
-        return -1;
+        fill_query_result_from_stmt(stmt, &out_array[count++]);
     }
 
     sqlite3_finalize(stmt);
-    sqlite3_close(db);
-    *out_count = count;
+    sqlite_unlock();
 
-    if (count == 0)
+    if (rc != SQLITE_DONE && rc != SQLITE_ROW)
     {
-        ESP_LOGW(TAG, "No cached record found: SN=%s, pushed=%d", sn, pushed ? 1 : 0);
         return -1;
     }
 
-    return 0;
+    *out_count = count;
+    return (count > 0) ? 0 : -1;
 }
 
 int QueryLatestRecordBySNAndPushState(const char *sn,
                                       bool pushed,
                                       QueryResult *out_result)
 {
-    if (out_result == NULL)
+    if (sn == NULL || sn[0] == '\0' || out_result == NULL)
     {
         return -1;
     }
 
     memset(out_result, 0, sizeof(QueryResult));
 
-    int count = 0;
-
-    if (QueryStructuredRecordsBySNAndPushState(sn, pushed, out_result, 1, &count) != 0)
+    if (!sqlite_lock()) return -1;
+    if (!sqlite_ready_locked())
     {
+        sqlite_unlock();
         return -1;
     }
 
-    return (count > 0) ? 0 : -1;
-}
-
+    /* 补发按IDNUM从小到大，避免乱序。 */
+    const char *sql =
+        "SELECT seq_no, sn, current_step, timestamp, pushed, value_data, created_at "
+        "FROM " SQLITE_CACHE_TABLE_NAME " "
+        "WHERE sn=? AND pushed=? "
+        "ORDER BY seq_no ASC, local_id ASC LIMIT 1;";
 
-/// @brief 兼容旧接口：查询结果写入全局变量 g_db1_result
-void query_db1_to_global(int target_id)
-{
-    memset(&g_db1_result, 0, sizeof(QueryResult));
-    (void)QueryStructuredRecordBySeq(target_id, &g_db1_result);
+    int ret = query_one_locked(sql, sn, -1, pushed ? 1 : 0, out_result);
+    sqlite_unlock();
+    return ret;
 }
 
-/// @brief 更新某条记录的推送状态
-/// @return 0: 成功，-1: 失败
-int UpdateRecordPushState(int seq_no, bool pushed)
+int QueryUnpushedRecords(QueryResult *out_array, int max_count, int *out_count)
 {
-    if (db1_name[0] == '\0')
+    if (out_array == NULL || out_count == NULL || max_count <= 0)
     {
-        snprintf(db1_name, sizeof(db1_name), "%s", DB_FILE_PATH);
+        return -1;
     }
 
-    sqlite3 *db = NULL;
-    sqlite3_stmt *stmt = NULL;
+    *out_count = 0;
+    memset(out_array, 0, sizeof(QueryResult) * max_count);
 
-    if (db_open(db1_name, &db) != SQLITE_OK)
+    if (!sqlite_lock()) return -1;
+    if (!sqlite_ready_locked())
     {
-        ESP_LOGE(TAG, "Failed to open database %s", db1_name);
+        sqlite_unlock();
         return -1;
     }
 
-    apply_sqlite_pragmas(db);
-
-    const char *sql = "UPDATE " SQLITE_CACHE_TABLE_NAME " SET pushed = ? WHERE seq_no = ?;";
+    const char *sql =
+        "SELECT seq_no, sn, current_step, timestamp, pushed, value_data, created_at "
+        "FROM " SQLITE_CACHE_TABLE_NAME " "
+        "WHERE pushed=0 ORDER BY local_id ASC LIMIT ?;";
 
-    int rc = sqlite3_prepare_v2(db, sql, -1, &stmt, NULL);
+    sqlite3_stmt *stmt = NULL;
+    int rc = sqlite3_prepare_v2(s_db, sql, -1, &stmt, NULL);
     if (rc != SQLITE_OK)
     {
-        ESP_LOGE(TAG, "prepare push-state update failed: %s", sqlite3_errmsg(db));
-        sqlite3_close(db);
+        sqlite_unlock();
         return -1;
     }
 
-    sqlite3_bind_int(stmt, 1, pushed ? 1 : 0);
-    sqlite3_bind_int(stmt, 2, seq_no);
+    sqlite3_bind_int(stmt, 1, max_count);
 
-    rc = sqlite3_step(stmt);
-    if (rc != SQLITE_DONE)
+    while (*out_count < max_count && (rc = sqlite3_step(stmt)) == SQLITE_ROW)
+    {
+        fill_query_result_from_stmt(stmt, &out_array[*out_count]);
+        (*out_count)++;
+    }
+
+    sqlite3_finalize(stmt);
+    sqlite_unlock();
+
+    return (rc == SQLITE_DONE || rc == SQLITE_ROW) ? 0 : -1;
+}
+
+/* -------------------------------------------------------------------------- */
+/* JSON兼容接口                                                                 */
+/* -------------------------------------------------------------------------- */
+
+static int serialize_value_array_for_storage(cJSON *value_item,
+                                             char *out_value_data,
+                                             size_t out_size)
+{
+    if (!cJSON_IsArray(value_item) || out_value_data == NULL || out_size == 0)
     {
-        ESP_LOGE(TAG, "push-state update failed: rc=%d, errmsg=%s", rc, sqlite3_errmsg(db));
-        sqlite3_finalize(stmt);
-        sqlite3_close(db);
         return -1;
     }
 
-    int changed = sqlite3_changes(db);
+    cJSON *storage_array = NULL;
+    cJSON *first = cJSON_GetArrayItem(value_item, 0);
 
-    sqlite3_finalize(stmt);
-    sqlite3_close(db);
+    if (first == NULL || cJSON_IsObject(first))
+    {
+        storage_array = cJSON_Duplicate(value_item, true);
+    }
+    else
+    {
+        storage_array = cJSON_CreateArray();
+        cJSON *group = cJSON_CreateObject();
+        cJSON *data = cJSON_CreateArray();
+        if (storage_array == NULL || group == NULL || data == NULL)
+        {
+            cJSON_Delete(storage_array);
+            cJSON_Delete(group);
+            cJSON_Delete(data);
+            return -1;
+        }
+
+        cJSON_AddStringToObject(group, "Name", "Legacy");
+        cJSON_AddItemToObject(group, "Data", data);
+        cJSON_AddItemToArray(storage_array, group);
 
-    if (changed <= 0)
+        int count = cJSON_GetArraySize(value_item);
+        for (int i = 0; i < count; i++)
+        {
+            cJSON *src = cJSON_GetArrayItem(value_item, i);
+            cJSON *item = cJSON_CreateObject();
+            if (item == NULL)
+            {
+                cJSON_Delete(storage_array);
+                return -1;
+            }
+
+            char name[24];
+            char value_string[32];
+            snprintf(name, sizeof(name), "Value%d", i);
+
+            if (cJSON_IsString(src) && src->valuestring != NULL)
+            {
+                snprintf(value_string, sizeof(value_string), "%s", src->valuestring);
+            }
+            else if (cJSON_IsNumber(src))
+            {
+                snprintf(value_string, sizeof(value_string), "%.2f", src->valuedouble);
+            }
+            else
+            {
+                cJSON_Delete(item);
+                cJSON_Delete(storage_array);
+                return -1;
+            }
+
+            cJSON_AddStringToObject(item, "name", name);
+            cJSON_AddStringToObject(item, "value", value_string);
+            cJSON_AddItemToArray(data, item);
+        }
+    }
+
+    char *json = cJSON_PrintUnformatted(storage_array);
+    cJSON_Delete(storage_array);
+    if (json == NULL)
     {
-        ESP_LOGW(TAG, "push-state update found no record: seq_no=%d", seq_no);
         return -1;
     }
 
+    size_t len = strlen(json);
+    if (len >= out_size)
+    {
+        free(json);
+        return -1;
+    }
+
+    memcpy(out_value_data, json, len + 1);
+    free(json);
     return 0;
 }
 
-/// @brief 查询未推送记录，供后续批量补发使用
-/// @return 0: 成功，-1: 失败
-int QueryUnpushedRecords(QueryResult *out_array, int max_count, int *out_count)
+int InsertJsonRecord(int seq_no, const char *sn, bool pushed, const char *json_data)
 {
-    if (out_array == NULL || out_count == NULL || max_count <= 0)
+    if (json_data == NULL)
     {
         return -1;
     }
 
-    *out_count = 0;
-    memset(out_array, 0, sizeof(QueryResult) * max_count);
-
-    if (db1_name[0] == '\0')
+    cJSON *root = cJSON_Parse(json_data);
+    if (root == NULL)
     {
-        snprintf(db1_name, sizeof(db1_name), "%s", DB_FILE_PATH);
+        return -1;
     }
 
-    sqlite3 *db = NULL;
-    sqlite3_stmt *stmt = NULL;
-
-    if (db_open(db1_name, &db) != SQLITE_OK)
+    cJSON *payload = cJSON_GetObjectItemCaseSensitive(root, "Data");
+    if (!cJSON_IsObject(payload))
     {
-        ESP_LOGE(TAG, "Failed to open database %s", db1_name);
-        return -1;
+        payload = root;
     }
 
-    apply_sqlite_pragmas(db);
+    const char *final_sn = sn;
+    int final_seq_no = seq_no;
+    int final_timestamp = 0;
+    int final_step = 0;
+    char value_data[DB_VALUE_DATA_MAX_LEN + 1] = {0};
 
-    const char *sql =
-        "SELECT seq_no, sn, current_step, timestamp, pushed, value_data, created_at "
-        "FROM " SQLITE_CACHE_TABLE_NAME " "
-        "WHERE pushed = 0 "
-        "ORDER BY created_at ASC, seq_no ASC "
-        "LIMIT ?;";
+    cJSON *sn_item = cJSON_GetObjectItemCaseSensitive(payload, "SN");
+    cJSON *id_item = cJSON_GetObjectItemCaseSensitive(payload, "IDNUM");
+    cJSON *timestamp_item = cJSON_GetObjectItemCaseSensitive(payload, "TIMESTAMP");
+    cJSON *step_item = cJSON_GetObjectItemCaseSensitive(payload, "CurrentStep");
+    cJSON *value_item = cJSON_GetObjectItemCaseSensitive(payload, "Value");
 
-    int rc = sqlite3_prepare_v2(db, sql, -1, &stmt, NULL);
-    if (rc != SQLITE_OK)
+    if ((final_sn == NULL || final_sn[0] == '\0') &&
+        cJSON_IsString(sn_item) && sn_item->valuestring != NULL)
     {
-        ESP_LOGE(TAG, "prepare unpushed query failed: %s", sqlite3_errmsg(db));
-        sqlite3_close(db);
-        return -1;
+        final_sn = sn_item->valuestring;
     }
 
-    sqlite3_bind_int(stmt, 1, max_count);
-
-    while ((rc = sqlite3_step(stmt)) == SQLITE_ROW)
+    if (final_seq_no < 0 && cJSON_IsNumber(id_item))
     {
-        fill_query_result_from_stmt(stmt, &out_array[*out_count]);
-        (*out_count)++;
+        final_seq_no = id_item->valueint;
+    }
 
-        if (*out_count >= max_count)
-        {
-            break;
-        }
+    if (cJSON_IsNumber(timestamp_item)) final_timestamp = timestamp_item->valueint;
+    if (cJSON_IsNumber(step_item)) final_step = step_item->valueint;
+
+    if (serialize_value_array_for_storage(value_item, value_data, sizeof(value_data)) != 0)
+    {
+        cJSON_Delete(root);
+        return -1;
     }
 
-    if (rc != SQLITE_DONE && rc != SQLITE_ROW)
+    if (final_sn == NULL || final_sn[0] == '\0' || final_seq_no < 0 || final_timestamp <= 0)
     {
-        ESP_LOGE(TAG, "unpushed query step failed: rc=%d, errmsg=%s", rc, sqlite3_errmsg(db));
-        sqlite3_finalize(stmt);
-        sqlite3_close(db);
+        cJSON_Delete(root);
         return -1;
     }
 
-    sqlite3_finalize(stmt);
-    sqlite3_close(db);
-    return 0;
+    int ret = InsertStructuredRecord(final_seq_no,
+                                     final_sn,
+                                     final_step,
+                                     final_timestamp,
+                                     pushed,
+                                     value_data);
+    cJSON_Delete(root);
+    return ret;
 }
 
+/* -------------------------------------------------------------------------- */
+/* 初始化入口                                                                   */
+/* -------------------------------------------------------------------------- */
 
 void SqLite_Init(void)
 {
     ESP_LOGI(TAG, "Initializing SqLite");
 
+    if (s_sqlite_mutex == NULL)
+    {
+        s_sqlite_mutex = xSemaphoreCreateMutex();
+        if (s_sqlite_mutex == NULL)
+        {
+            ESP_LOGE(TAG, "Failed to create SQLite mutex");
+            return;
+        }
+    }
+
     esp_err_t ret = mount_fatfs_storage();
     if (ret != ESP_OK)
     {
-        ESP_LOGE(TAG, "FATFS mount failed");
+        ESP_LOGE(TAG, "FATFS mount failed; SQLite is disabled to preserve existing data");
         return;
     }
 
     snprintf(db1_name, sizeof(db1_name), "%s", DB_FILE_PATH);
-    memset(&g_db1_result, 0, sizeof(QueryResult));
+    memset(&g_db1_result, 0, sizeof(g_db1_result));
 
-    // print_real_flash_size();
-    // print_all_partitions();
-    // print_fatfs_usage();
+    if (!sqlite_lock())
+    {
+        return;
+    }
+
+    int rc = init_database_locked();
+    sqlite_unlock();
 
-    init_databases();
+    if (rc != SQLITE_OK)
+    {
+        ESP_LOGE(TAG,
+                 "SQLite initialization failed: rc=%d. No database file was automatically deleted.",
+                 rc);
+    }
 
     esp_log_level_set(TAG, ESP_LOG_INFO);
 }
